Click "HDS_Feed"
(*Click "Proto-Q"*)

if ImageFound("okButton")
	Click FoundImageLocation()
end if

waitfor 10, "AdvancedTab"
(*Click "AdvancedTab"*)

if ImageFound("okButton")
	Click FoundImageLocation()
end if

(*
repeat until imageFound("LoaderURL")
	click "Scrollbar"
end repeat


click "LoaderURL"
typeText commandKey, "a"

typetext backspace

typeText "/player/prime/?v=2.0.0-5"


(*click the last item of EveryImageLocation("Setbutton")*)

click "LoaderSetbutton"

click "RefreshButton"
*)


click "BandwidthMetrics"
wait 2
click "DisplayMetrics"
wait 2
click "RenditionMgmt"
(*
if ImageFound("PlayButtonLg01", "PlayButtonLg") 
	Click FoundImageLocation()
end if*)

moveto ("Rendition3", "Rendition4")
moveto "upcomingNaN"
moveto ("maxRendition3", "maxRendition4")


Click "PlayerControlPanel"
put the shouldrepositionmouse into oldSRPM
set the shouldrepositionmouse to NO
moveto "fullScreenBUtton"

click "PlayerSizeSmall"
moveto "upcomingRendition0"
(*moveto "maxRendition0"*)

(*waitfor 5, "shiftDown"*)
waitfor 20, ("Rendition1", "Rendition0")
moveto ("Rendition1", "Rendition0")

Click "PlayerControlPanel"
put the shouldrepositionmouse into oldSRPM
set the shouldrepositionmouse to NO
moveto "fullScreenBUtton"

click "PlayerSizeLarge"
(*moveto "maxRendition6"
moveto "upcomingRendition6"*)
(*waitfor 5, "shiftUp"*)
waitfor 20, ("Rendition1", "Rendition0")
moveto ("Rendition6", "Rendition7")

Click "PlayerControlPanel"
put the shouldrepositionmouse into oldSRPM
set the shouldrepositionmouse to NO
moveto "HD"
wait 2
moveto "fullScreenBUtton"
click "PlayerSizeMedium"












wait 20

if ImageFound("upcomingRendition2") 
	Click FoundImageLocation()
end if

wait 10

if ImageFound("upcomingRendition2") 
	moveTo FoundImageLocation()
end if

Click "PlayerControlPanel"
put the shouldrepositionmouse into oldSRPM
set the shouldrepositionmouse to NO

if ImageFound("Rendition02") 
	moveTo FoundImageLocation()
end if

wait 10

if ImageFound("Rendition02") 
	moveTo FoundImageLocation()
end if

Click "PlayerControlPanel"
put the shouldrepositionmouse into oldSRPM
set the shouldrepositionmouse to NO

wait 10

moveto "Rendition02"



(*
MoveTo "Bitratelimiter"
MoveTo "UpcomingRendition"
MoveTo "MaxFittingRendition"

*)


(*click "ScrollbarTop"

Click "PlayButtonLg01"*)
(*Click "PlayButtonLg"*)

(*
put the shouldrepositionmouse into oldSRPM
set the shouldrepositionmouse to NO
Click "PlayButtonPanel"
Click "PauseButton"
drag "SliderNeedle"
Drop "EndTimer"
(*Drop "EndTimerQ"*)
Click "PlayButton"
WaitFor 10, "ReplayButton"
MoveTo "ReplayButton"
*)
